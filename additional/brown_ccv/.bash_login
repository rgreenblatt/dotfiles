#!/bin/bash

if [ -z "$PROFILE_SOURCED" ]; then
  source .profile
fi

if [ "$SHELL" != "~/.local/bin/zsh" ]; then
  export SHELL="~/.local/bin/zsh"
fi

if hash module; then
  module load 'java/8u111'
  module load 'python/3.6.6'
  module load 'intel/2017.0'
  exec nvim -l
fi
